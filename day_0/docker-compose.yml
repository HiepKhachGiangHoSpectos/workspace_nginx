version: '3.8'

services:
  nginx:
    build: .
    container_name: my_nginx
    ports:
      - "8080:80"   # truy cập localhost:8080 trên Windows
    volumes:
      - ./html:/usr/share/nginx/html:ro
      - ./default.conf:/etc/nginx/conf.d/default.conf:ro
