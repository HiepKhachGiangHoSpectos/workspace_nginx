version: '3.8'
services:
  nginx:
    build: ./nginx
    container_name: nginx_gzip_cache
    ports:
      - "8080:80"    # truy cập http://localhost:8080
    volumes:
      - ./html:/usr/share/nginx/html:ro
